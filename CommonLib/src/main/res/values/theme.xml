<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 默认弹窗样式-->
    <style name="DefaultDialogStyle" parent="Theme.MaterialComponents.DayNight.Dialog">
        <!-- 窗口背景色（透明）-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否有覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="dialog_transparent" parent="Theme.MaterialComponents.DayNight.Dialog">
        <!-- 窗口背景色（透明）-->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- 透明Activity -->
    <style name="TranslucentActivity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>